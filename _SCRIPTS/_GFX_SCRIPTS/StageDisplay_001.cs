using System.Collections;
using System.Collections.Generic;
using UnityEngine;


/*

    StageDisplay is a live, continually-updating script that reads the information in game.roundManager.round.stage
    it then draws on screen everything it needs to.
    As much as possible, the Round and Stage state should not be dependent on what the game engine is doing
    rather the other way around
*/
public class StageDisplay : MonoBehaviour
{

    public GameManager game;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
